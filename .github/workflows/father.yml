name: inevitable

on:
  push:

jobs:
  immeasurable:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        penetrate: ["real", "motor", "cordial", "concert", "decline"]
        thirsty: ["establish", "rude", "novel", "upward"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar Pk.jar
